<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- Required: App Sandbox -->
    <key>com.apple.security.app-sandbox</key>
    <true/>

    <!-- Network: outbound connections to AI APIs, GitHub, SSH -->
    <key>com.apple.security.network.client</key>
    <true/>

    <!-- Network: inbound for local HTTP/WebSocket server -->
    <key>com.apple.security.network.server</key>
    <true/>

    <!-- File access: user-selected directories via NSOpenPanel / security-scoped bookmarks -->
    <key>com.apple.security.files.user-selected.read-write</key>
    <true/>

    <!-- File access: ~/Downloads for import/export workflows -->
    <key>com.apple.security.files.downloads.read-write</key>
    <true/>

    <!-- V8 JIT compilation (required by Electron/Chromium) -->
    <key>com.apple.security.cs.allow-jit</key>
    <true/>

    <!-- V8 unsigned executable memory -->
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>

    <!-- Load native .node modules (node-pty, DuckDB) not signed by Apple -->
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>

    <!-- DYLD env vars (fix-path, native module resolution) -->
    <key>com.apple.security.cs.allow-dyld-environment-variables</key>
    <true/>

    <!-- Security-scoped bookmarks: persist user-granted filesystem access across launches -->
    <key>com.apple.security.files.bookmarks.app-scope</key>
    <true/>
</dict>
</plist>
