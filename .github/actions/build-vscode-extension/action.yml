name: "Build VS Code Extension"
description: "Build the Lattice VS Code extension"

runs:
  using: "composite"
  steps:
    - name: Build VS Code extension
      shell: bash
      run: make vscode-ext
