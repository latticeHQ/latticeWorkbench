# LiveKit Cloud credentials (same as configured in workbench Settings → Providers → LiveKit)
LIVEKIT_URL=wss://your-project.livekit.cloud
LIVEKIT_API_KEY=APIxxxxxxxxxxxxxxx
LIVEKIT_API_SECRET=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# OpenAI (for LLM + TTS)
OPENAI_API_KEY=sk-...

# Deepgram (for STT — get a free key at https://console.deepgram.com)
DEEPGRAM_API_KEY=...

# ── Optional: Anam avatar video ───────────────────────────────────────────────
# Set ANAM_API_KEY to enable a talking-head avatar video rendered by the agent.
# The workbench will display it in the video tile alongside your self-view.
# Get a key at: https://app.anam.ai
# Also install: pip install 'livekit-agents[anam]~=1.3'
#
# ANAM_API_KEY=...
# ANAM_PERSONA_ID=...   # optional — uses default persona if not set
